---
description: Export the current Strudel code with header comments
argument-hint: ""
---

Export the current Strudel code with header comments showing the date and layer names.

## Your Task

The user wants to export their current mix code.

## Workflow

IMPORTANT: Use the MCP tool directly as `mcp__dj-claude__export_code` — do NOT use Bash or search for files.

1. Call `mcp__dj-claude__export_code` (no parameters needed).
2. Display the exported code in a code block so the user can copy it.
3. If nothing is playing, suggest playing some music first.

## Example

- `/dj-claude:export`
